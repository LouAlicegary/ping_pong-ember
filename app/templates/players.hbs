<h3>Players</h3>
{{#each model as |player|}}
    <div class="row">
        <div class="col-md-3">
            {{player.name}}
        </div>
        <div class="col-md-1">
            {{player.sigma}}
        </div>
        <div class="col-md-1">
            {{player.mu}}
        </div>    
    </div>
{{/each}}