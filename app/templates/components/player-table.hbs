<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Total</th>
            <th>Singles</th>
            <th>Doubles</th>
            <th>Rating</th>
        </tr>
    </thead>
    <tbody>

        {{#each players as |player|}}
            {{#link-to 'players.show' player.id tagName="tr"}}
                <td>{{player.name}}</td>
                <td>{{player.total_wins}}-{{player.total_losses}}</td>
                <td>{{player.singles_wins}}-{{player.singles_losses}}</td>
                <td>{{player.doubles_wins}}-{{player.doubles_losses}}</td>
                <td>{{player.mu}}</td>
            {{/link-to}}
        {{/each}}
    </tbody>
</table>


{{yield}}