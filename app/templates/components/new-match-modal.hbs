<button class="destroy button" {{action "toggleModal"}}>ADD MATCH</button>

{{#if isShowingModal}}
  
  {{#modal-dialog close="toggleModal" targetAttachment="center" translucentOverlay=true}}

    <div class="container">
        <div class="row">
            The new match
        </div>
        <div class="row">
            {{input type="text" id="new-name" placeholder="Name" value=newName enter="submitMatch"}}
        </div>
    </div>

  {{/modal-dialog}}

{{/if}}

{{yield}}
